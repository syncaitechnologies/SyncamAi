# Executable traceability

`requirements.json` maps every canonical MVP requirement to an owner, implementation phase, task, contract, verification placeholder, and release gate. `tasks.json` is the active backlog added after the historical roadmap catalog.

The sprint prose in `ENGINEERING-EXECUTION-ROADMAP-SyncCam-AI.md` is historical planning evidence. Active order is dependency-driven from this directory. Existing T-0001–T-0300 identifiers remain stable; new work starts at T-0301.
